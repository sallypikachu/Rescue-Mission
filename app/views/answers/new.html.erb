<h2>Submit a new answer for <%= @question.title %></h2>

<%= form_for([@question, @answer]) do |f| %>
  <p>
    <%= f.label :answer %><br>
    <%= f.text_area :answer, size: "50x5" %>
  </p>

  <p>
    <%= f.submit "Submit Answer" %>
  </p>
<% end %>
